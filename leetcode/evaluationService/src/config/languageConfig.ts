import { CPP_IMAGE, PYTHON_IMAGE } from "../util/contants";

export const languageConfig = {
    PYTHON : {
        timeout : 2000,
        imageName : PYTHON_IMAGE
    },
    CPP : {
        timeout : 1000,
        imageName : CPP_IMAGE
    }
}